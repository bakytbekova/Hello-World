#include <stdio.h>
#include <locale.h>
int main ()
{
int d;
float L, Pi= 3.14;
setlocale(0, "rus");
printf("Введите диаметр окружност:d");
scanf_s("%d", &d);
L = Pi*d;
printf("Длина окружности = %.2f/n", L);
return 0
}
